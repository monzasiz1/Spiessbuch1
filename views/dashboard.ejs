<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>Dashboard – <%= user.username %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <header>
    <img src="/logo.png" alt="Logo" class="logo">
    <h1>Willkommen, <%= user.username %></h1>
  </header>

  <div class="container">
    <section>
      <p class="punkte">💰 Gesamtkosten: <%= punkte.toFixed(2) %> €</p>

      <% if (strafen && strafen.length > 0) { %>
        <table>
          <thead>
            <tr>
              <th>Datum</th>
              <th>Veranstaltung</th>
              <th>Grund</th>
              <th>Kosten (€)</th>
            </tr>
          </thead>
          <tbody>
            <% strafen.forEach(s => { %>
              <tr>
                <td data-label="Datum"><%= s.datum %></td>
                <td data-label="Veranstaltung"><%= s.veranstaltung %></td>
                <td data-label="Grund"><%= s.grund %></td>
                <td data-label="Kosten"><%= parseFloat(s.punkte).toFixed(2) %> €</td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } else { %>
        <p class="info">Keine Strafen vorhanden.</p>
      <% } %>
    </section>
  </div>

  <footer>
    <a href="/dashboard/export" class="btn">📄 PDF herunterladen</a>
    <a href="/logout" class="btn btn-secondary">🚪 Logout</a>
    <% if (user.role === 'admin') { %>
      <a href="/admin" class="btn btn-admin">🛠️ Adminbereich</a>
    <% } %>
  </footer>
</body>
</html>
